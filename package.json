{
  "scripts": {
    "install": "cd portfolio && npm install",
    "start": "cd portfolio && npm run start",
    "build": "cd portfolio && npm run build",
    "test": "echo 'pass test'",
    "deploy": "cd portfolio && npm run deploy"
  }
}
